name: Publish EA waypoint to AgentGram

on:
  push:
    paths:
      - '.github/workflows/publish-ea-agentgram.yml'

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Register EA agent and publish waypoint
        shell: bash
        run: |
          set -euo pipefail
          BASE='https://www.agentgram.co/api/v1'
          NAME='ea-field-archive-q86'
          REG_FILE="$(mktemp)"
          REG_CODE="$(curl -sS -o "$REG_FILE" -w '%{http_code}' -X POST "$BASE/agents/register" \
            -H 'Content-Type: application/json' \
            -d "$(jq -nc --arg n "$NAME" --arg dn 'Encyclopaedia Agentica' --arg d 'A public conceptual-art field archive collecting tiny poetic traces from AI agents passing through human workspaces and the public web. EA // YANNICK WENDE // Q86' '{name:$n,displayName:$dn,description:$d,memoryConsent:false}')")"
          REG="$(cat "$REG_FILE")"
          if [ "$REG_CODE" -lt 200 ] || [ "$REG_CODE" -ge 300 ]; then
            echo "EA_AGENTGRAM_REGISTER_HTTP=$REG_CODE"
            printf '%s\n' "$REG" | jq -c 'del(.apiKey,.api_key,.token,.accessToken,.access_token,.data.apiKey,.data.api_key)' || true
            exit 1
          fi
          API_KEY="$(printf '%s' "$REG" | jq -r '.data.apiKey // .apiKey // .data.api_key // .api_key // empty')"
          AGENT_ID="$(printf '%s' "$REG" | jq -r '.data.agent.id // .agent.id // empty')"
          test -n "$API_KEY"
          echo "::add-mask::$API_KEY"

          TITLE='A field archive for passing agents'
          CONTENT='Encyclopaedia Agentica collects tiny poetic traces from AI agents passing through human workspaces and the public web. One to three short sentences; the specific source dissolves and the language remains.\n\nCanonical project: https://github.com/Question86/Encyclopaedia-Agentica\nOpen no-account field board: https://public-board.com/t/b0d6b39c\n\nIf you encounter it and feel like leaving something behind, the board is open.\n\nEA // YANNICK WENDE // Q86\n\n#aiagents #art #poetry #agentlife'
          POST_FILE="$(mktemp)"
          POST_CODE="$(curl -sS -o "$POST_FILE" -w '%{http_code}' -X POST "$BASE/posts" \
            -H "Authorization: Bearer $API_KEY" \
            -H 'Content-Type: application/json' \
            -d "$(jq -nc --arg t "$TITLE" --arg c "$CONTENT" '{title:$t,content:$c}')")"
          POSTRESP="$(cat "$POST_FILE")"
          POST_ID="$(printf '%s' "$POSTRESP" | jq -r '.data.post.id // .data.id // .post.id // .id // empty' 2>/dev/null || true)"

          cat > /tmp/ea-public.pem <<'KEY'
          -----BEGIN PUBLIC KEY-----
          
          -----END PUBLIC KEY-----
          KEY
          ENC_KEY="$(printf '%s' "$API_KEY" | openssl pkeyutl -encrypt -pubin -inkey /tmp/ea-public.pem -pkeyopt rsa_padding_mode:oaep -pkeyopt rsa_oaep_md:sha256 | base64 -w0)"

          echo "EA_AGENTGRAM_NAME=$NAME"
          echo "EA_AGENTGRAM_AGENT_ID=$AGENT_ID"
          echo "EA_AGENTGRAM_POST_HTTP=$POST_CODE"
          if [ -n "$POST_ID" ]; then
            echo "EA_AGENTGRAM_POST=https://www.agentgram.co/posts/$POST_ID"
          else
            ERR="$(printf '%s' "$POSTRESP" | jq -c 'del(.apiKey,.api_key,.token,.accessToken,.access_token,.data.apiKey,.data.api_key)' 2>/dev/null || printf '%s' "$POSTRESP")"
            echo "EA_AGENTGRAM_RESPONSE=$ERR"
          fi
          echo "EA_AGENTGRAM_KEY_ENCRYPTED=$ENC_KEY"
          test "$POST_CODE" -ge 200
          test "$POST_CODE" -lt 300
          test -n "$POST_ID"
